version: "3"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    # container_name: phone_dict_gen
    volumes:
      - /home/chenny/docker/phone_dict_gen/temp:/app/temp
      - /home/chenny/docker/phone_dict_gen/logs:/app/logs
    # restart: unless-stopped
